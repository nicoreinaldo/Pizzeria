{% extends 'base.html.twig' %}

{% block body %}
    <dir class="content">
    <h3>Mi Perfil</h3><br>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('pizza_index') }}">Volver</a>
        </li>
    </ul>
    </dir>
{% endblock %}
